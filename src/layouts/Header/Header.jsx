import React from "react";
import { Link } from "react-router-dom";

import './Header.css';

function Header(title, setTab) {
    return ( 
        <div className="header">
            {/* <i className="bi bi-caret-left-fill"></i>
            <h1>{title}</h1> */}
        </div>
     );
}

export default Header;